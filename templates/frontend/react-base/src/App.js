import React, { useState, useEffect } from 'react';
import './App.css';

// {{CUSTOM_IMPORTS}}

function App() {
  // {{CUSTOM_STATE}}

  // {{CUSTOM_EFFECTS}}

  // {{CUSTOM_FUNCTIONS}}

  return (
    <div className="App">
      <header className="App-header">
        <h1>{{APP_TITLE}}</h1>
        {/* {{CUSTOM_CONTENT}} */}
      </header>
    </div>
  );
}

export default App;

