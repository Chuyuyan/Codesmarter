# 🧪 文件上传功能测试步骤

## ✅ 测试准备已完成

- ✅ 测试项目已创建：`test_upload_project/`
- ✅ ZIP 文件已创建：`test_project.zip` (0.43 KB)
- ✅ 测试脚本已创建：`test_upload_api.py`

---

## 📋 测试方法 1：网页界面测试（推荐）

### 步骤：

1. **启动服务器**
   ```bash
   python -m backend.app
   ```
   等待看到：`* Running on http://127.0.0.1:5050`

2. **打开浏览器**
   - 访问：`http://127.0.0.1:5050/`

3. **登录账户**
   - 如果没有账户，先注册
   - 登录后应该看到主页

4. **测试上传功能**
   - 找到 "📚 Index Repository" 部分
   - 应该默认显示 **"📤 Upload Folder (ZIP)"** 标签页
   - 点击 "选择文件" 或文件输入框
   - 选择 `test_project.zip`（在当前目录）
   - （可选）输入仓库名称：`test-project`
   - 点击 **"Upload and Index"** 按钮

5. **查看结果**
   - ✅ 成功：显示绿色成功消息，包含：
     - "Successfully uploaded and indexed!"
     - Chunk 数量
     - Repository 名称
   - ❌ 失败：显示红色错误消息

6. **验证索引**
   - 在 "Ask Questions" 部分提问：
     - "What does the hello function do?"
     - "Show me the main.py file"
   - 应该能获得答案和代码引用

---

## 📋 测试方法 2：API 测试脚本

### 步骤：

1. **启动服务器**
   ```bash
   python -m backend.app
   ```

2. **获取认证 Token**
   - 打开浏览器：`http://127.0.0.1:5050/`
   - 登录账户
   - 按 F12 打开开发者工具
   - 在 Console 中输入：
     ```javascript
     localStorage.getItem('authToken')
     ```
   - 复制返回的 token

3. **运行测试脚本**
   ```bash
   python test_upload_api.py
   ```
   - 输入刚才复制的 token
   - 等待测试完成

4. **查看结果**
   - ✅ 成功：显示 "Upload successful!"
   - ❌ 失败：显示错误信息

---

## 🔍 检查点

### 前端检查：
- [ ] 标签页切换是否正常？
- [ ] 文件选择器是否只接受 .zip？
- [ ] 文件大小验证是否工作（尝试上传 >100MB 的文件应该失败）？
- [ ] 上传按钮是否显示加载状态？
- [ ] 成功/错误消息是否显示？

### 后端检查：
- [ ] 服务器日志是否显示上传请求？
- [ ] ZIP 文件是否成功解压？
- [ ] 索引是否成功创建？
- [ ] 文件是否保存在 `data/uploads/` 目录？

### 功能检查：
- [ ] 上传后能否正常提问？
- [ ] 代码引用是否准确？
- [ ] 仓库是否关联到用户账户？

---

## 🐛 常见问题

### 问题 1：服务器未运行
**错误：** `Connection refused` 或 `Failed to fetch`
**解决：** 启动服务器：`python -m backend.app`

### 问题 2：认证失败
**错误：** `401 Unauthorized` 或 `Authorization header missing`
**解决：** 确保已登录，token 有效

### 问题 3：文件格式错误
**错误：** `Only ZIP files are supported`
**解决：** 确保上传的是 .zip 文件

### 问题 4：文件太大
**错误：** `File too large. Maximum size is 100MB`
**解决：** 压缩文件，删除 `node_modules` 等大文件夹

### 问题 5：ZIP 文件损坏
**错误：** `Invalid ZIP file`
**解决：** 重新压缩文件夹

---

## 📊 预期结果

### 成功上传后：
```json
{
  "ok": true,
  "repo_id": "test-project-api",
  "chunks": 3,
  "extracted_path": "data/uploads/abc12345/test_upload_project",
  "repo_name": "test-project-api"
}
```

### 服务器日志示例：
```
[upload_and_index] Saved zip file: data/uploads/abc12345/test_project.zip
[upload_and_index] Extracting zip file...
[upload_and_index] Extracted to: data/uploads/abc12345
[upload_and_index] Repository root: data/uploads/abc12345/test_upload_project
[upload_and_index] Starting indexing...
[upload_and_index] Found 3 chunks
[upload_and_index] Index built successfully
[upload_and_index] Success: {...}
```

---

## ✅ 测试完成检查清单

- [ ] 上传 ZIP 文件成功
- [ ] 文件成功解压
- [ ] 索引成功创建
- [ ] 可以正常提问
- [ ] 代码引用准确
- [ ] 仓库关联到用户
- [ ] 错误处理正常（无效文件、大文件等）

---

## 🎯 下一步

如果测试成功，可以：
1. 尝试上传真实项目
2. 测试大文件上传（接近 100MB）
3. 测试多个仓库上传
4. 测试提问功能

如果测试失败，请：
1. 检查服务器日志
2. 检查浏览器控制台错误
3. 查看错误消息详情
4. 报告具体错误信息

